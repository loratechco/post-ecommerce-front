const nextConfig = {
  i18n: {
    locales: ["en", "fa", "fr"],
    defaultLocale: "en",
    localeDetection: true,
  },
  images: {
    domains: ["betaspedire.b-cdn.net"],
  },
};

export default nextConfig;
